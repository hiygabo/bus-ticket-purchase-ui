version: '3.0'
services:
  frontend:
    build: .
    container_name: bus_frontend
    ports:
      - "5173:5173"
    volumes:
      - ./:/app
      - /app/node_modules
    environment:
      - CHOKIDAR_USEPOLLING=true